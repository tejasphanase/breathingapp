import { CircleShadowDirective } from './circle-shadow.directive';

describe('CircleShadowDirective', () => {
  it('should create an instance', () => {
    const directive = new CircleShadowDirective();
    expect(directive).toBeTruthy();
  });
});
